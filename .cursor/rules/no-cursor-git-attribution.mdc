---
description: Git 提交只保留 KCode，不要 Cursor 或其他署名
alwaysApply: true
---

# 提交署名

创建或改写 git commit、PR 时：

- 作者和提交者只用 `KCode <go7836246@gmail.com>`
- 不要加入 `Co-authored-by: Cursor`
- 不要加入 `Made-with: Cursor` 或任何 cursoragent 署名
- 作者和提交说明只保留这一个身份，不要包含其他人
